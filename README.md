# Masters & Studios

Платформа для связи инструкторов wellness-направлений (йога, фитнес, танцы и др.) со студиями и фитнес-центрами. Находите замены на занятия, расширяйте клиентскую базу и развивайте бизнес.

## ✨ Возможности

### Для преподавателей
- ✅ Создание профиля с указанием дисциплин и станций метро
- ✅ Выбор из 20+ популярных wellness-дисциплин + возможность указать свою
- ✅ Указание стоимости занятий и типа работы (групповые/индивидуальные)
- ✅ Отображение опыта и квалификации
- ✅ Загрузка фотографии профиля с предпросмотром
- ✅ Полная валидация всех полей формы
- ✅ Dashboard с картой актуальных запросов на замены
- ✅ Просмотр локаций студий на интерактивной карте
- ✅ Нижняя навигация для быстрого доступа к функциям

### Для студий
- ✅ Полная регистрация профиля с фотографиями и адресом
- ✅ Интерактивная карта для выбора местоположения студии
- ✅ Автоматическое определение адреса через геокодер
- ✅ Создание запросов на поиск преподавателей
- ✅ Указание требований к занятиям (дата, время, место, оплата)
- ✅ Детальное описание требований и особенностей
- ✅ Dashboard с картой доступных инструкторов поблизости
- ✅ Нижняя навигация для удобного управления
- 🔄 Управление заявками от преподавателей (в разработке)

### Технические особенности
- ✅ Современный стек: Next.js 14 + TypeScript + Tailwind CSS
- ✅ Полная типизация TypeScript
- ✅ Валидация форм с использованием Zod
- ✅ Переиспользуемые UI компоненты
- ✅ API роуты для работы с данными
- ✅ Готовая интеграция с Supabase
- ✅ Интеграция Yandex Maps API 3.0 для интерактивных карт
- ✅ Геокодирование адресов (автоматическое определение)
- ✅ Выбор локации на карте с обратной геокодировкой
- ✅ Адаптивный дизайн
- ✅ Оптимизированная производительность

## 🚀 Технологический стек

- **Frontend**: Next.js 14, React 18, TypeScript
- **Styling**: Tailwind CSS, clsx, tailwind-merge
- **Maps**: Yandex Maps API 3.0
- **Backend**: Supabase (PostgreSQL)
- **Validation**: Zod
- **Deployment**: Vercel (рекомендуется)

## 📁 Структура проекта

```
masters-studios-app/
├── app/                      # Next.js App Router
│   ├── api/                 # API routes
│   │   ├── instructors/    # Instructor API endpoints
│   │   └── job-requests/   # Job request API endpoints
│   ├── instructor/         # Instructor pages
│   │   └── page.tsx       # Instructor profile form
│   ├── studio/            # Studio pages
│   │   └── page.tsx      # Job request creation
│   ├── layout.tsx         # Root layout
│   ├── page.tsx          # Homepage
│   └── globals.css       # Global styles
├── components/            # React components
│   ├── ui/               # Reusable UI components
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Select.tsx
│   │   ├── FileUpload.tsx
│   │   ├── MultiSelect.tsx
│   │   └── Toast.tsx
│   ├── YandexMap.tsx     # Interactive map for displaying locations
│   ├── YandexMapPicker.tsx # Map picker for selecting location
│   ├── BottomNav.tsx     # Bottom navigation
│   └── Navbar.tsx        # Top navigation component
├── lib/                   # Utility libraries
│   ├── api/              # API client functions
│   ├── constants/        # App constants (disciplines, metro stations)
│   ├── types/           # TypeScript types
│   ├── utils/           # Helper functions
│   ├── validations/     # Zod validation schemas
│   └── supabaseClient.ts # Supabase client
├── supabase/             # Database schema
│   └── schema.sql       # SQL schema for Supabase
├── .env.example         # Environment variables template
├── tsconfig.json        # TypeScript configuration
├── tailwind.config.js   # Tailwind CSS configuration
├── next.config.js       # Next.js configuration
└── package.json         # Dependencies
```

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 18+
- npm или yarn
- Аккаунт Supabase (бесплатный tier достаточен)

### Шаги установки

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd masters-studios-app
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   ```bash
   cp .env.example .env.local
   ```

   Отредактируйте `.env.local` и добавьте свои credentials:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
   NEXT_PUBLIC_YANDEX_MAPS_API_KEY=your_yandex_maps_api_key
   ```

   **Получение API ключа Yandex Maps:**
   - Зарегистрируйтесь на [Yandex Developer](https://developer.tech.yandex.ru/)
   - Перейдите в раздел "JavaScript API и HTTP Геокодер"
   - Создайте новый ключ для Maps API 3.0
   - Скопируйте ключ и добавьте в `.env.local`

4. **Настройте базу данных Supabase**
   - Создайте новый проект в [Supabase](https://supabase.com)
   - Перейдите в SQL Editor
   - Скопируйте содержимое файла `supabase/schema.sql`
   - Выполните SQL скрипт

5. **Запустите проект в режиме разработки**
   ```bash
   npm run dev
   ```

6. **Откройте браузер**
   ```
   http://localhost:3000
   ```

## 📦 Сборка для продакшена

```bash
npm run build
npm start
```

## 🔌 API Routes

### Instructors

- `GET /api/instructors` - Получить всех инструкторов
- `POST /api/instructors` - Создать профиль инструктора

### Job Requests

- `GET /api/job-requests` - Получить все запросы
- `GET /api/job-requests?status=open` - Фильтр по статусу
- `POST /api/job-requests` - Создать запрос

## 🎨 UI Компоненты

Все UI компоненты находятся в `components/ui/` и поддерживают:
- TypeScript типизацию
- Кастомизацию через className
- Валидацию и обработку ошибок
- Accessibility (ARIA)

**Button**
```tsx
<Button variant="primary" size="lg" isLoading={loading}>
  Сохранить
</Button>
```

**Input**
```tsx
<Input
  label="Email"
  type="email"
  error={errors.email}
  helperText="Введите ваш email"
/>
```

**Select**
```tsx
<Select
  label="Выберите опцию"
  options={[{ value: '1', label: 'Опция 1' }]}
  error={errors.option}
/>
```

**MultiSelect**
```tsx
<MultiSelect
  label="Дисциплины"
  options={DISCIPLINES}
  value={selected}
  onChange={setSelected}
/>
```

**FileUpload**
```tsx
<FileUpload
  label="Фото"
  accept="image/*"
  preview={true}
  onChange={(file) => setFile(file)}
/>
```

## ✅ Валидация

Проект использует Zod для валидации данных. Схемы находятся в `lib/validations/`.

**Пример использования:**
```typescript
import { instructorSchema } from '@/lib/validations/instructor';

try {
  const validated = instructorSchema.parse(formData);
  // Данные валидны
} catch (error) {
  // Обработка ошибок валидации
}
```

## 🗄️ База данных

### Таблицы

1. **instructors** - Профили преподавателей
2. **studios** - Профили студий
3. **job_requests** - Запросы на поиск преподавателей
4. **applications** - Заявки от преподавателей на запросы

Полная схема с индексами и триггерами в файле `supabase/schema.sql`.

## 🚀 Развертывание

### Vercel (рекомендуется)

1. Push код в GitHub
2. Импортируйте проект в Vercel
3. Добавьте переменные окружения
4. Deploy!

### Другие платформы

Проект совместим с любой платформой, поддерживающей Next.js:
- Netlify
- Railway
- Render
- DigitalOcean App Platform

## 🗺️ Roadmap

- [ ] Аутентификация пользователей (Supabase Auth)
- [ ] Система уведомлений (email/push)
- [ ] Чат между студиями и инструкторами
- [ ] Календарь занятий
- [ ] Рейтинг и отзывы
- [ ] Мобильное приложение (React Native)
- [ ] Интеграция с платежными системами
- [ ] Расширенная аналитика

## 🤝 Вклад в проект

Приветствуются pull requests! Для крупных изменений сначала создайте issue для обсуждения.

## 📄 Лицензия

MIT

## 💬 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте существующие issues
2. Создайте новый issue с описанием проблемы
3. Приложите логи и скриншоты

---

Создано с ❤️ для wellness-сообщества
